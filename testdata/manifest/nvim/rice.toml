schema_version = 1
name = "nvim"
description = "Neovim configuration"
supported_os = ["linux", "darwin"]
target = "$HOME/.config/nvim"
profile_key = "os"

[profiles.linux]
sources = ["common", "linux"]

[profiles.darwin]
sources = ["common", "darwin"]
